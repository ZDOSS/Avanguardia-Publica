# Strict AI Agent Rules

1. **READ THE FULL REVIEW BODY**: You must always read the entire PR review body and every part of the Greptile block in full. Do NOT rely solely on the confidence score, Greptile Summary, check label, or inline comments; the body contains the most up-to-date fix information.
2. **FINISH OPEN PRS**: If you are working on an open PR, push fixes to its existing branch and finish the PR. Do NOT abandon an open PR to create a new one unless explicitly instructed.
3. **NEW PRS OFF MAIN**: For completely new features or changes, fetch `origin/main`, create the task branch directly from that ref, and open the PR against `main`. Do not synchronize local `main` unless the active goal authorizes it.
4. **STOP ASSUMING**: Stop deliberating and assuming any workflows. Follow the user's instructions literally. Do not invent new branches or workflows out of thin air to "correct" things unless ordered to.
5. **DCO AND CRYPTOGRAPHIC SIGNING ARE DISTINCT**: Every commit requires DCO signoff (`-s` or `--signoff`). Cryptographic signing (`-S`) is optional and does not replace DCO. Do not create, transfer, or export signing keys merely to change a verification label, and never place signing material in the repository. If cryptographic verification is requested but is not configured, report that clearly; the user manages signing keys and GitHub verification outside the repository.
6. **NO CREDENTIAL FILES IN REPO**: NEVER create, export, or store any GPG keys, SSH keys, or credential files within the repository directory under any circumstances, even if they are public keys.
7. **CHECK PR MERGE STATUS**: Before pushing fixes to an open PR, always check if the PR has already been merged. If it has been merged, check out a new branch off main, commit your new changes there, and open a new PR instead of pushing to the closed/merged branch.
8. **GIT BRANCH AND LOCK SAFETY**: Branch new work from a freshly fetched `origin/main` without synchronizing local `main` unless the active goal authorizes it. If Git reports a lock, first identify any live Git process; do not blindly delete the lock or keep retrying. Use a task-specific Codex-owned worktree only when the normal checkout is genuinely unavailable.
9. **GOAL-SCOPED PR AUTHORITY**: Merge a PR and synchronize local `main` only when the user has explicitly delegated those actions for an active goal. Keep one PR open, check pending Greptile reviews no more often than every five minutes, and merge only after a current 5/5 Greptile review whose **Last reviewed commit** matches `headRefOid`, no actionable findings, passing required checks, and no conflict. Afterward, verify `MERGED` on GitHub, the merge commit in `origin/main`, and a clean fast-forwarded local `main`. Outside such a goal, the user alone merges PRs and synchronizes `main`.
